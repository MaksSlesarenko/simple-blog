{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <div class="post">
        <p><small>{{ post.createdAt.date }}</small></p>
        <p>{{ post.title }}</p>
        <p>{{ post.body }}</p>
    </div>
{% endblock %}
